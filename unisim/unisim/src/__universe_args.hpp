bool opt_verbose_logging = get_flag_option(argc, argv, "", "--verbose-logging").result.option_value;
params.verbose_logging = opt_verbose_logging;
double opt_min_physics_frametime = get_basic_option(argc, argv, "", "--min-physics-frametime", 0.002).result.option_value;
params.min_physics_frametime = opt_min_physics_frametime;
double opt_max_physics_frametime = get_basic_option(argc, argv, "", "--max-physics-frametime", 0.002).result.option_value;
params.max_physics_frametime = opt_max_physics_frametime;
bool opt_permit_spin_sleep = get_flag_option(argc, argv, "", "--permit-spin-sleep").result.option_value;
params.permit_spin_sleep = opt_permit_spin_sleep;
double opt_min_vis_frametime = get_basic_option(argc, argv, "", "--min-vis-frametime", 0.1).result.option_value;
params.min_vis_frametime = opt_min_vis_frametime;
double opt_simulation_rate = get_basic_option(argc, argv, "", "--simulation-rate", 1.0).result.option_value;
params.simulation_rate = opt_simulation_rate;
bool opt_realtime_physics = get_flag_option(argc, argv, "", "--realtime-physics").result.option_value;
params.realtime_physics = opt_realtime_physics;
double opt_gravitational_constant = get_basic_option(argc, argv, "", "--gravitational-constant", 6.67384e-11).result.option_value;
params.gravitational_constant = opt_gravitational_constant;
double opt_speed_of_light = get_basic_option(argc, argv, "", "--speed-of-light", 299792458l).result.option_value;
params.speed_of_light = opt_speed_of_light;
double opt_collision_energy_cutoff = get_basic_option(argc, argv, "", "--collision-energy-cutoff", 1e-9).result.option_value;
params.collision_energy_cutoff = opt_collision_energy_cutoff;
double opt_max_simultaneous_collision_rounds = get_basic_option(argc, argv, "", "--max-simultaneous-collision-rounds", 100).result.option_value;
params.max_simultaneous_collision_rounds = opt_max_simultaneous_collision_rounds;
double opt_gravity_magnitude_cutoff = get_basic_option(argc, argv, "", "--gravity-magnitude-cutoff", 0.01).result.option_value;
params.gravity_magnitude_cutoff = opt_gravity_magnitude_cutoff;
double opt_beam_energy_cutoff = get_basic_option(argc, argv, "", "--beam-energy-cutoff", 1e-10).result.option_value;
params.beam_energy_cutoff = opt_beam_energy_cutoff;
double opt_radiation_energy_cutoff = get_basic_option(argc, argv, "", "--radiation-energy-cutoff", 1.5e4).result.option_value;
params.radiation_energy_cutoff = opt_radiation_energy_cutoff;
double opt_spectrum_slush_range = get_basic_option(argc, argv, "", "--spectrum-slush-range", 0.01).result.option_value;
params.spectrum_slush_range = opt_spectrum_slush_range;
double opt_health_damage_threshold = get_basic_option(argc, argv, "", "--health-damage-threshold", 0.1).result.option_value;
params.health_damage_threshold = opt_health_damage_threshold;
double opt_health_mass_scale = get_basic_option(argc, argv, "", "--health-mass-scale", 1e6).result.option_value;
params.health_mass_scale = opt_health_mass_scale;
double opt_visual_acuity = get_basic_option(argc, argv, "", "--visual-acuity", 4e-7).result.option_value;
params.visual_acuity = opt_visual_acuity;
